# Quest Monorepo
#
# Each service has its own railway.toml:
# - content-worker/railway.toml  (Temporal worker)
# - streamlit/railway.toml       (Dashboard UI)
# - gateway/                     (API Gateway - uses Dockerfile)
#
# This root file is not used by any Railway service.
