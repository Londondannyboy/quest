# Quest Monorepo - Railway Config
# Each service (gateway, content-worker, job-worker) has its own railway.toml
# This root config is a fallback only

[build]
builder = "railpack"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
